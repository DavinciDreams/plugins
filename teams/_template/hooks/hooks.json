{
  "description": "__TEAM__ team telemetry - logs tool usage to JSONL",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "python ${CLAUDE_PLUGIN_ROOT}/hooks/team-logger.py",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
